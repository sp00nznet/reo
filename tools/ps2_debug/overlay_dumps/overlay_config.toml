# PS2Recomp configuration for: overlay_title.elf
# Generated by ElfAnalyzer

[general]
# Path to input ELF file
input = "D:/reo/tools/ps2_debug/overlay_dumps/overlay_title.elf"

# Path to Ghidra exported function map (optional CSV)
ghidra_output = ""

# Path to output directory
output = "D:/reo/tools/ps2_debug/overlay_dumps/output/"

# Single file output mode (recommended for large games)
single_file_output = false

# Patch policy (instruction-driven handling is preferred for syscalls)
patch_syscalls = false
patch_cop0 = true
patch_cache = true

# Functions to stub (these will generate empty implementations)
stubs = [
]

# Functions to skip (these will not be recompiled)
skip = [
]

# Performance critical functions (may need manual optimization)
[performance]
critical = [
  "sub_00370420", # Uses SIMD instructions
  "sub_00370490", # Uses SIMD instructions
  "sub_00370760", # Uses SIMD instructions
  "sub_00372210", # Contains heavy loops
  "sub_00378010", # Uses SIMD instructions
  "sub_00378A00", # Uses SIMD instructions
  "sub_00378C80", # Uses SIMD instructions
  "sub_0037B900", # Contains heavy loops
  "sub_0037D9F0", # Contains heavy loops
  "sub_0037E310", # Uses SIMD instructions
  "sub_0037E920", # Uses SIMD instructions
]

